dist: focal
language: c

addons:
  apt:
    packages:
      - libssl-dev

script:
  - export PATH="/opt/at14.0/bin:$PATH"
  - make -j2 qemu
  - ./lib/libtpm/test.sh

jobs:
  include:
    - os: linux
      arch: ppc64le
